[Unit]
Description=Ephemeral tmpfs for /var/home
DefaultDependencies=no
Conflicts=umount.target
Before=local-fs.target umount.target
After=swap.target

[Mount]
What=tmpfs
Where=/var/home
Type=tmpfs
Options=mode=0755,uid=0,gid=0,size=16G,nosuid,nodev

[Install]
WantedBy=local-fs.target
