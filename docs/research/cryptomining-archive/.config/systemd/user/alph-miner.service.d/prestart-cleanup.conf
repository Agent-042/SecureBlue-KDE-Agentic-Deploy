[Service]
# Kill any stale Rigel processes left by an unclean previous run before the
# new container starts.  The leading '-' makes systemd ignore any failure.
ExecStartPre=-/home/agent-42/alph-miner-cleanup.sh
